# 2026-04-19

## 凌晨研究日誌

### 2026-04-19

| 研究檔案 | 卡片名 | 狀態 | 摘要 |
|----------|--------|------|------|
| `docker是什麼怎麼用.md` | docker是什麼?怎麼用? | ✅ 完成 | （见完整研究报告）  📌 old-topic（第2次研究） |

## What I Learned
- Cognee MiniMax integration uses `LLM_PROVIDER=custom` + `LLM_ENDPOINT` (not `anthropic` which was wrong adapter)
- Ollama local embedding (Qwen3-Embedding-0.6B) replaces MiniMax embedding due to `vectors` format incompatibility
- Cognee four "limitations" were mostly documentation issues — SDK already supports raw text, CHUNKS mode is pure vector search, verbose=True returns original chunks
- Only concurrent write is real Kuzu lock limitation
- Disk cleanup freed ~5.6G: whisper-env/ (4.9G) + Docker backups (684M) + old backup/*.md

## Notes

### Cognee MiniMax LLM Integration Breakthrough
- **Problem**: `LLM_PROVIDER=anthropic` + `ANTHROPIC_BASE_URL` went through wrong adapter (LiteLLM couldn't handle)
- **Solution**: `LLM_PROVIDER=custom` + `LLM_MODEL=openai/MiniMax-M2.7` + `LLM_ENDPOINT=https://api.minimax.io/v1`
- Architecture: `Cognee → GenericAPIAdapter → LiteLLM → MiniMax API`
- Verification: search response ✅ (1.67s / 4.99s)

### Ollama Embedding (replacing MiniMax)
- **Problem**: MiniMax embedding returns `vectors` format (not OpenAI's `data`), LiteLLM can't parse
- **Solution**: `EMBEDDING_PROVIDER=ollama` + `OLLAMA_EMBEDDING_MODEL=Qwen/Qwen3-Embedding-0.6B` + `EMBEDDING_DIMENSIONS=1024`
- Model pulled (639MB), `embed_text()` test 1024 dims ✅

### Cognee Vector DB Switch: LanceDB → PGVector
- **Reason**: LanceDB persistence bug (`add()` reports success but collection is empty, 0 rows)
- **Vector dimension incompatibility**: Old collection 3072-dim (MegaBite), Ollama is 1024-dim, deletion failed
- **PGVector Docker** started at `:5432`, psycopg2-binary installed

### Cognee Four "Limitations" Clarified
| Feature | Status | Explanation |
|---------|--------|-------------|
| Raw text input | ✅ SDK works | REST API requires file upload, but SDK `cognee.add("text")` supports raw |
| Pure vector search (no LLM) | ✅ CHUNKS works | `SearchType.CHUNKS` bypasses LLM |
| Fast original text retrieval | ✅ CHUNKS + verbose | Returns full chunk list with original text |
| Concurrent write | ❌ Kuzu lock | Real architectural limitation |

### Five Search Modes
| SearchType | LLM | Returns |
|------------|-----|---------|
| `GRAPH_COMPLETION` | ✅ | LLM summarized answer |
| `RAG_COMPLETION` | ✅ | LLM answer based on chunks |
| `CHUNKS` | ❌ | Raw chunk list |
| `CHUNKS_LEXICAL` | ❌ | Keyword match |
| `SUMMARIES` | partial | Pre-generated summaries |

### System Architecture
```
Cognee API Server (:8000)
├── PGVector (:5432)     ← vector storage
├── Kuzu (embedded)       ← knowledge graph
├── Ollama (:11434)      ← embedding (Qwen3-Embedding-0.6B)
└── MiniMax API           ← LLM (openai/MiniMax-M2.7)
```

### Cognee Complete Guide
- Created: `others/cognee/COGNEE_COMPLETE_GUIDE.md`
- Includes Feynman learning method explanation, architecture diagram, feature table

### System Cleanup
- **whisper-env/ deleted**: 4.9G freed (independent venv, no service引用)
- **Docker backups**: kept Apr 16 + 19, deleted 3 old (684M freed)
- **Old backup/*.md**: deleted 42 × `*_2026-03-*.md` files (~60K)
- **ChromaDB container**: stopped and removed (Cognee uses PGVector)
- **Total freed**: ~5.6G

### Cron Job Fixes
| Job | Name | Fix |
|-----|------|-----|
| `85a00b06` | OpenClaw Release Tracker | `to="channel:1484346353269342380"` (was user ID) |
| `7581765f` | Config Backup | same fix |
| `e2a069c0` | GitHub Trending | Step 5 changed to exec+Python ✅ |

### System Status (16:04)
| Service | Status |
|---------|--------|
| OpenClaw Gateway | ✅ port 18789 |
| Cognee API Server | ✅ port 8000 |
| Ollama | ✅ port 11434 |
| PGVector | ✅ port 5432 |
| Qdrant | ✅ port 6333-6334 |
| ChromaDB | ❌ removed |

### Daily Operation Summary Cron
- Job ID: `e15d4164`
- Timeout adjusted: 300s → 600s
- Reason: 04-18 cron timeout, context inflation

## Related Knowledge
- [Work-in-Midnight System](../knowledge/openclaw-automation/work-in-midnight-system.md)

## Tags
#cognee #minimax-integration #ollama #pgvector #disk-cleanup #cron-fixes

## Resources
- Cognee complete guide: `.../openclaw-workspace/others/cognee/COGNEE_COMPLETE_GUIDE.md`
- Cognee env records: `.../openclaw-workspace/others/cognee/cognee-env-records.md`