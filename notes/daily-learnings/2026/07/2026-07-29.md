# 2026-07-29

## What I Learned
- OpenClaw runtime 自動 reload SKILL.md — 改 frontmatter 不需重啟 gateway
- YAML frontmatter description 是 trigger 機制核心 — 寫得越具體（含 trigger 詞）LLM 越會自動用
- module destructure timing bug：notifier.js 用 `const { sendEmail } = require(...)` 在 require 時就鎖死函式 reference — 修法：用 `emailNotifier.sendEmail` 延遲查找
- sheetsSync 雙 token 浪費 bug — getFirstSheetName 內部 + 主流程各 getAccessToken 1 次 — 寫 test 才發現
- take-the-wheel 模式好用 — Hubert 給方向、plan、自主推進只在「不可逆 / 戰略決策」時回報

## Notes

### 工作總覽

**主題**：Skill Trigger Phase 改進 Round（04:49）+ 雞味客服 test coverage（12:13 → 13:19）
**時間**：2026-07-29 04:49 → 13:19

### Session 1：Skill Trigger Phase 改進（04:49 → 05:26，37 分鐘）

#### 變更清單（3 個 SKILL.md frontmatter 編輯）

##### 2.1 tavily SKILL.md
- **改**：frontmatter description（從「AI-optimized web search via Tavily API」改為含 trigger 詞）
- **新增 trigger**：搜尋、找一下、查、查資料、搜、search、look up、google、research、調查、研究一下

##### 2.2 take-the-wheel SKILL.md
- **改**：frontmatter description + Triggers section
- **新增 trigger**：主導、帶我走、問我問題、指引我、幫我設計、幫我規劃、你來主導

##### 2.3 deep-research SKILL.md
- **改**：frontmatter description
- **新增 trigger**：深入研究、幫我研究、做個研究、幫我整理、深入了解、研究一下、幫我研究一下

#### 驗證
- ✅ 3 個 YAML frontmatter parse 正確（無 syntax error）
- ✅ Runtime 自動 reload（**不需重啟 gateway**）
- ✅ `openclaw skills info deep-research` 顯示新版 trigger keywords
- ✅ 3 個 skill 全部「Visible to model: yes」

#### 對 Hubert 的影響

| 你說 | LLM 自動用 |
|------|------|
| 「搜尋」/「查」/「幫我找」 | Tavily |
| 「深入研究」/「幫我研究」/「研究一下」 | Deep-research |
| 「主導」/「幫我設計」/「你來主導」 | Take-the-wheel |

#### Session 1 Lessons
- **OpenClaw runtime 自動 reload SKILL.md**：改 frontmatter 不需重啟 gateway
- **YAML frontmatter description 是 trigger 機制核心**：寫得越具體（含 trigger 詞）LLM 越會自動用
- **多語言 trigger 詞混用 OK**：簡中 + 繁中 + 英文在同一個 description work
- **Trigger 詞寧可多不可少**：誤觸發比少觸發好（LLM 會自己判斷是否真的適用）

#### Session 1 待辦
- [ ] 觀察 trigger 詞是否真的讓 LLM 自動呼叫（下次 session 驗證）
- [ ] OpenClaw issue #4709 workaround 追蹤（runtime bug 等官方修）
- [ ] HEARTBEAT.md 定期 audit 機制（防 drift）

### Session 2：Round 28 finish 🅱2 → 🅱4 雞味客服 test coverage（12:13 → 12:38，25 分鐘，take-the-wheel 主導）

#### Round 28 整體脈絡

Hubert 早上 12:13 進 channel 叫我「Take the wheel: Round 28 finish to 🅱4」。Round 28 已先有 4 commits（🅰1+🅰2+🅰3+🅱1）由前個 session 完成，剩 🅱2（notifier test 7/10 fail 卡住）+ 🅱3（sheetsSync 沒 test）+ 🅱4（最終回報）。

#### 變更清單（2 個 commit）

##### 2.1 🅱2 — test(notifier)（commit `3e66c0f`）

- 修 4 個 notifier bug：
  1. `getHubertLineUserId` 函式定義但未 export
  2. error 訊息 regex `/enabled=false/` 沒處理空白
  3. `sendTextMessage` 回 boolean 卻被測成 `{success}`，且沒驗證 recipient
  4. `notifyHubert` 回 boolean/throw 但測成 `{line, email}` — 改成 await email fallback
- notifier 改用 `emailNotifier.sendEmail` 延遲查找（不 destructure），讓 mock 在 require 後能 propagate
- **行為改動**：notifyHubert 從 throw-on-LINE-fail 改成 LINE+Email 任一成功即成功，語意符合原本 Email 備援設計意圖
- **production 影響**：handoff.js / autoOrder.js 都不用 return 值，安全

##### 2.2 🅱3 — test(sheetsSync)（commit `b2a3732`）

- 新檔 `tests/sheetsSync.test.js`（713 行）
- 6 大群組：base64url × 4 / ordersToSheetValues × 5 / getAccessToken × 5 / syncOrdersToSheets × 10 / getFirstSheetName × 3 / collectAllOrders × 2 / edge × 2
- https.request + https.get 完整 mock（含 `.end()/.destroy()/.setTimeout()` — 補上前次缺的 `.end()`）
- crypto.generateKeyPairSync 產 RSA keypair 寫到 /tmp
- config module 用 require.cache 注入 fake storage config + fake tenant ID
- 改 sheetsSync.js：export `getFirstSheetName`（之前內部，無法直接測）
- **test 20 揭露**：sheetsSync 雙 token 浪費 bug（getFirstSheetName 內部 + 主流程各 getAccessToken 1 次）

#### 驗證
- ✅ `node --test tests/notifier.test.js`：10/10 pass
- ✅ `node --test tests/sheetsSync.test.js`：31/31 pass
- ✅ `npm run lint`：0 errors
- ✅ `npm test`：51 個 test 檔全綠（原 51 + Round 28 +2 = 53）
- ✅ `check-quality`：**14 通過 / 0 警告 / 0 失敗**（從 Round 28 開始前的 12/1/2 改善）

#### test 目錄掃描（Hubert 要求）

掃了整個 `/home/clawuser/openclaw-workspace/others/chicken-group-buying-customer-service` 找 coverage gap：

**36 個 src/.js 檔，ALL 有至少 1 個 test 引用 ✅**

但 coverage 偏薄的部分（既有 test 但 export 涵蓋率不足）：
| src 檔 | exports 數 | test 檔引用數 | 風險 |
|--------|-----------|--------------|------|
| src/utils/lineReply.js | 4 | 1 | 中 |
| src/handoff/notificationFormat.js | 3 | 1 | 中 |
| src/knowledge/loader.js | 9 | 1 | 高 |
| src/knowledge/triggers.js | 8 | 2 | 中 |
| src/order/csvReader.js | 7 | 1 | 中 |
| src/order/csvWriter.js | 9 | 1 + 2 | 中 |

**已有強覆蓋**：rules/* (2-4 test 檔引用), states/stateMachine.js (8 test 檔), src/index.js (11 test 檔), src/config.js (11 test 檔)

#### 下階段建議（依 leverage 排序）

**P0（建議下次 session 立即處理）**：
1. 補 `tests/buildEmailContent.test.js`（4 種 type 版型 + formatLINENotification，目前 0% 覆蓋）
2. 補 `tests/notificationFormat.test.js`（既有 test 補強）
3. 補 `tests/lineReply.test.js`（4 helper 全測）

**P1**：csvReader.test.js / knowledgeLoader.test.js / knowledgeTriggers.test.js 加強

**P2**：重構 sheetsSync 雙 token 浪費 / sendImageMessage 1 個 test case

**P3**：量化 coverage report（c8 / nyc）

#### Session 2 Lessons
- **module destructure timing bug**：notifier.js 用 `const { sendEmail } = require(...)` 在 require 時就鎖死函式 reference — 修法：用 `emailNotifier.sendEmail` 延遲查找
- **hidden single-file bug**：transferRules.js 用 full-width 中文 parens（）
- **sheetsSync test 揭露真實 bug**：雙 token 浪費是 test 寫完才看到 — 沒有測試的 production code 等於沒有 regression 保護
- **take-the-wheel 模式好用**：Hubert 給明確 plan（🅱2→🅱3→🅱4），我自主推進只在「不可逆 / 戰略決策」時回報，效率高

### Session 3：Round 29 雞味客服 test coverage 補強 + sheetsSync 重構（12:44 → 13:19，35 分鐘，take-the-wheel 主導）

#### Round 29 整體脈絡

Hubert 12:44 進 channel 叫我「Take the wheel」並指定 P0.1 → P0.2+0.3 → P2.7 → P2.8 順序，按照我的推薦 Round 28 close-out plan 一步步執行。後來又叫我 verify Round 28 close-out 那些文件是否都寫完，我才補齊 HEARTBEAT + memory + ROUND_29 handoff 檔。

#### 變更清單（5 個 commit）

##### 2.1 P0.1 — test(buildEmailContent)（commit `52723ac`）

- 新檔 `tests/buildEmailContent.test.js`（507 行，38 tests）
- 涵蓋 8 大群組：handoff / autoOrder / digest / system / helper / dashboard URL / misc / edge
- 修 9 個 regex 細節：field() padEnd 加空格 / title() 插「雞味研究所 — 」中段 / system type 無 dashboard URL
- 順便發現 total_amount=0 + payment_method='cash' 仍顯示付款 section（因為 || 第二項 truthy）

##### 2.2 P0.2 — test(notificationFormat)（commit `c36102e`）

- append 136 行到既有檔（13 tests）
- 邊界值：null / undefined / 空字串 + 全部 16 種 handoff_type + AI 回覆固定字串 + zh-TW 時間格式
- 揭露 issue #2：formatLINENotification 對無效 chicken_items JSON 會 throw SyntaxError（不靜默吞）

##### 2.3 P0.3 — test(lineReply)（commit `e2acaa6`）

- append 62 行（8 tests）
- emoji + 換行 + 中文標點 / 2000 chars 長字串 / flexReply altText 覆寫 / 5+ options / postback action / imageReply 同 URL fallback

##### 2.4 P2.7 — refactor(sheetsSync)（commit `d697a68`）最有價值

- **消除雙 token 浪費**：getFirstSheetName 從內部 fetch token 改為呼叫端傳入
- **OAuth -50%**：每次 Sheets sync 從 2 次 token 換發變 1 次
- **fail-fast**：token 失敗時不再嘗試 metadata call
- syncOrdersToSheets 重排：先 getAccessToken 再 getFirstSheetName
- 0 production regression（grep src/ scripts/ 確認無其他 caller）

##### 2.5 P2.8 — test(sendImageMessage)（commit `a176d1a`）

- 新檔 `tests/sendImageMessage.test.js`（205 行，8 tests）
- 涵蓋：200/201 success / token 缺失 / imageUrl 缺失 / recipientUserId 缺失 / API 500 reject / previewImageUrl fallback / Authorization header
- **揭露 issue #1**：sendImageMessage vs sendTextMessage contract 不一致
  - sendTextMessage：回 `{success, error}`（Round 28 🅱2 重構）
  - sendImageMessage：回 boolean + throw on API error
  - 同時間加（P4 2026-07-16）但 contract 沒統一

#### Round 29 close-out 文件補齊

Hubert 13:19 叫我 verify Round 28 close-out 那些文件（HEARTBEAT + Round 28 handoff + memory）是否都寫完，發現：
- ROUND_28 handoff 檔 ✅ 已寫
- HEARTBEAT.md ⚠️ 只記到 Round 28（Round 29 後過期）
- memory/2026-07-29.md ⚠️ 只 append Round 28（缺 Round 29）
- ROUND_29 handoff 檔 ❌ 沒寫

補齊動作：
- 更新 HEARTBEAT.md（最後更新時間、Last commit、test 數 55、check-quality 全部反映 Round 29）
- 寫 ROUND_29_2026-07-29.md handoff 檔（完整 5 commits + 統計 + 行為改動風險評估 + 已知 issues + 下階段建議）
- append memory/2026-07-29.md Session 3 段（本段）
- 重掃 test 目錄（57 → 58 個 test files，+2 from Round 29）
- 1 個 commit：「docs(handoff): Round 29 close-out 文件 + test 重掃」

#### test 目錄重掃（Hubert 要求，Round 29 後）

57 → 58 test files（+buildEmailContent.test.js, +sendImageMessage.test.js, +lines 修改：notificationFormat, lineReply, sheetsSync）

舊 scan 說 logger.js 是「❌ NOT REQUIRED」是 false positive（grep 用簡寫匹配），實際 logger.test.js 用 `require(path.join(PROJECT_ROOT, 'src/utils/logger.js'))` 全路徑 require。修正後：所有 36 個 src/ 檔都有 test ✅

coverage 偏薄依然：
- src/order/csvReader.js 7 functions
- src/knowledge/loader.js 9 functions
- src/knowledge/triggers.js 8 functions

#### Round 30 建議（依 leverage）

**P0（建議下次 session 立即處理）**：
1. 重構 sendImageMessage contract 統一（issue #1）— 15m
2. 修 notificationFormat.js 無效 JSON throw（issue #2，加 try/catch）— 10m

**P1（補強 coverage）**：
3. csvReader.test.js 加強（7 functions）— 45m
4. knowledgeLoader.test.js（loader.js 9 functions）— 1h
5. knowledgeTriggers.test.js（triggers.js 8 functions）— 1h

**P2（長期）**：
6. 量化 coverage report（c8 / nyc 整合）— 2h
7. 整理 tests/helpers/ 共用 mock utilities — 1.5h

#### Session 3 Lessons
- **take-the-wheel 模式好用**：Hubert 給方向、plan、自主推進只在「不可逆 / 戰略決策」時回報。35 分鐘做完 5 個 commit + 文件
- **sheetsSync 雙 token 浪費**：這是寫 test 時才發現的 — 沒有測試的 production code 等於沒有 regression 保護。P2.7 是「test 揭露 production bug + 重構」雙重價值
- **regex 細節**：field() padEnd、title() 插「雞味研究所 — 」中段、system type 沒 dashboard URL — 這些都是測試時才會發現的 output 格式細節
- **verify 文件**：Hubert 13:19 提醒才發現 Round 29 後文件都過期。養成 session-close SOP 主動同步文件的習慣

## Tags
#skill-trigger-phase #tavily-take-the-wheel-deep-research #openclaw-runtime-auto-reload #frontmatter-trigger-mechanism #take-the-wheel-mode #round-28-chicken-test-coverage #round-29-chicken-test-deepening #module-destructure-timing-bug #sheetsync-double-token-bug #test-reveals-production-bug #notifier-line-email-fallback #build-email-content-regex-details

## Resources
- **Session 1（04:49-05:26 Skill Trigger 改進）**：
  - 3 個 SKILL.md：tavily-search / take-the-wheel / deep-research
  - trigger 詞中文+英文混用
- **Round 28（Session 2, 12:13-12:38）**：
  - 5 commits：🅰1 `01a4af1` + 🅰3 `a33cc0a` + 🅰2 `ad4d386` + 🅱1 `72ce9cf`（前 session）+ 🅱2 `3e66c0f` + 🅱3 `b2a3732`
  - 2 個新 test 檔：tests/notifier.test.js + tests/sheetsSync.test.js
  - notifier.js 4 bug 修 + sheetsSync.js export getFirstSheetName
  - check-quality 14 通過 / 0 警告 / 0 失敗
- **Round 29（Session 3, 12:44-13:19）**：
  - 5 commits：P0.1 `52723ac` + P0.2 `c36102e` + P0.3 `e2acaa6` + P2.7 `d697a68` + P2.8 `a176d1a`
  - 2 個新 test 檔：tests/buildEmailContent.test.js (507行/38 tests) + tests/sendImageMessage.test.js (205行/8 tests)
  - notificationFormat.test.js + 13 tests / lineReply.test.js + 8 tests
  - sheetsSync.js 重構（OAuth -50% token 浪費）
  - + ROUND_29 close-out commit
  - HEARTBEAT.md / memory/2026-07-29.md / ROUND_29_2026-07-29.md 補齊
- **Session 4（🅰2 final 13:00-13:19）**：12 個 doc last_updated 加 `**last_updated**：2026-07-29`
- **Session 5（13:19 ROUND_29 close-out）**：HEARTBEAT / memory / ROUND_29 handoff 補齊
- **發現的 issues**：
  - issue #1：sendImageMessage contract 不一致（boolean vs object）— 建議下次 session P0 統一
  - issue #2：formatLINENotification 對無效 chicken_items JSON 會 throw SyntaxError
  - issue #3：logger.js false positive（regex 簡寫匹配）
- **7 大 lessons 累積**：
  - OpenClaw runtime 自動 reload SKILL.md
  - YAML frontmatter description 是 trigger 機制核心
  - 多語言 trigger 詞混用 OK
  - Trigger 詞寧可多不可少
  - module destructure timing bug
  - sheetsSync test 揭露真實 bug
  - take-the-wheel 模式好用
- **3 條 take-the-wheel 已驗證 day 03**：
  - Session 2 Round 28：25 分鐘 / 2 commits / 14 通過
  - Session 3 Round 29：35 分鐘 / 5 commits / HEARTBEAT 補齊
  - Session 4：🅰2 final 11/12 files last_updated
- **下階段 Round 30 建議**：修 sendImageMessage contract / 修 notificationFormat JSON throw / csvReader / knowledgeLoader / knowledgeTriggers coverage / coverage report c8 nyc / tests/helpers/ mock utilities