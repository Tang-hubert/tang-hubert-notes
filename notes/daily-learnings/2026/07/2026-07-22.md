# 2026-07-22

## What I Learned
- Node:test 標準轉換 pattern 易踩 syntax：`require('node:test'` 漏右括號 → 轉換完每個檔立即 `node -c` 驗證
- Production crash 已存在的隱患：`orderFormatter.js` exports `formatOrderSummary`，但 `src/index.js` + `confirming.js` require undefined `formatCustomerReply` — 加 alias 一次修兩個問題
- LLM 自己推理時間邏輯是 anti-pattern — hard-call `validateDate` + 列 7 reject 路徑比 prompt 更可靠
- .netrc 取代 cmdline 明文密碼（`ps aux` 看得到 `-u user:pass`）— `curl -n` 從 `~/.netrc` 讀（mode 600）
- TESTING_SOP 不要 `date -d '+7 days'`：開團日是固定 `open_dates` 清單，不是「今天 +7 天」邏輯

## Notes

### 工作總覽

**Session**: `agent:main:discord:channel:1512213273846485058`
**時間範圍**: 2026-07-20 22:30 → 2026-07-22 23:08 GMT+8
**Duration**: 2d 12h+（約 60+ 小時長時間 session）
**最終狀態**: session close-out 完成，Sign C-all **48/48 ✅** / Sign D explore 完成 / Sign E deferred

### 完整時間軸

| 時間 (GMT+8) | 事件 |
|------------|------|
| 07-20 22:30 | Hubert sign-on "OK 執行" 11 個 sign-on options |
| 07-20 22:30 → 07-21 23:18 | **Round 1+2**：Bug #1 cascade + P0 #1-#4 + P1 B14/B16 + 文件清理 + 資安（12 commits）|
| 07-21 23:18 | 暫停（Hubert 休息），部分 commit 已推送 |
| 07-22 06:43 | 11 commits pushed 完成 |
| 07-22 06:55 → 07:00 | **Sign B done**：`wrangler deploy` 推 Worker Bug #1 fix v `683f6f9b-ec22-4c1b-b96e-a6b5f39b974c` |
| 07-22 07:06 | HANDOFF.md 更新（Sign B 狀態反映） |
| 07-22 08:55 → 22:30 | **Sign C-all**：統一 test framework 48/48 套（8 commits + 1 amend）|
| 07-22 22:50 | Memory Archive cron 跑完 |
| 07-22 23:00 | 每日操作總結 cron 跑（測試中） |
| 07-22 23:07 | **Hubert @ 收尾指令**（直接 @brt1122claw）|
| 07-22 23:10 | `npm run lint:fix` 修 logger.test.js multi-space |
| 07-22 23:15 | 寫本文檔 + 更新 4 個狀態檔 + commit + push + rsync |

### Round 1+2：11 個 Sign-on 任務執行（Hubert 22:30 sign-on）

| 階段 | Bug/Task | Status | Commit |
|------|----------|--------|--------|
| Round 1 | Bug #1 (B08/B10/B13/B17 cascade) | ✅ | `98151cf` + `e5f8564` + `23091c4` + Worker `e245eea` |
| Round 1 | P0 #1 Dashboard 按鈕 (B05+B07) | ✅ | `53ea4b6` → `8704387` (4 amends) |
| Round 1 | P0 #2 解除轉真人按鈕 (B11) | ✅ | `0a9214a` |
| Round 1 | P0 #3 轉真人 reply 簡短 (B12) | ✅ verify only | default 已 brief,no commit |
| Round 1 | P0 #4 LLM 日期邏輯 (B09) | ✅ | `6dabe71`（main_idea.md + sync-canonical）|
| Round 1 | P1 B14 轉帳戶名移除 | ✅ verify only | config 沒戶名 field |
| Round 1 | P1 B16 訂單確認前要列完整 | ✅ | `6dabe71`（**CRITICAL：同時發現 production crash**，加 alias `formatCustomerReply`）|
| Round 2 | P2 TESTING_GUIDE.md doc fix | ✅ | `810c91b` |
| Round 2 | P2 HANDOFF.md §5 update | ✅ | `810c91b` |
| Round 2 | P2 memory append | ✅ | `810c91b` |
| Round 2 | 資安 §0.5 .netrc 章節 | ✅ | `810c91b` |

### Sign B done — Worker deploy v `683f6f9b`（07-22 07:00）

- `wrangler deploy` 把 commit `e245eea` 推上 Cloudflare production
- Version ID: `683f6f9b-ec22-4c1b-b96e-a6b5f39b974c`
- Bug #1 雙邊 fix 生效（chicken.yaml + Worker `DEFAULT_IGNORED_KEYWORDS` 都移除「我要訂購」）
- 4 個 cascade bugs 全自動恢復 (B08/B10/B13/B17)
- HEARTBEAT.md + HANDOFF.md commit refs 已更新

### Sign C-all — 48/48 套 test framework 統一（07-22 08:55 → 22:30）

從自訂 assert 風格統一為 `node:test` 風格：

| Batch | Commit | 套數累計 | 內容 |
|-------|--------|---------|------|
| 第 1 批 | `51e3dcc` | 5/48 | date / timeUtils / state-trimmed-value / whitelist / address-handoff |
| 第 2 批 | `0a9cd0a` | 8/48 | +3 套 |
| 第 3 批 | `9adba15` | 13/48 | +5 套 |
| 第 4 批 (rules) | `51f4c3c` | 22/48 | +9 套 |
| 第 5 批 (states) | `595c557` | 26/48 | +4 套 |
| 第 6 批 | `9fe0726` | 32/48 | +6 套 |
| 第 7 批 | `097e657` | 40/48 | +8 套 |
| 第 8 批 | `a649467` | **48/48 ✅** | +3 套 |
| + amend | `5ca4aba` | 48/48 | 修 `tests/timezone.test.js` syntax error |

**Pattern 標準**：
```js
const { test } = require('node:test');
const assert = require('node:assert/strict');
test('name', () => { assert.equal(actual, expected); });
```

### Sign D 探索 — Worker FAQ 前處理（未執行，等下個 session）

- `external-user/cloudflare-worker/src/index.ts` line 100-115 有 `常見問題` keyword 在 `IGNORED_KEYWORDS`，但**沒有** `/api/knowledge` endpoint
- 知識源：`chicken.yaml` `knowledge.base_path` = `knowledge/tenants/chicken` 已備好
- 12 個知識檔案完整：01_product → 12_reply_examples + INDEX.md
- 預計實作：Worker `/api/knowledge?q=<keyword>` + keyword matching + canned reply BEFORE LLM
- 預估 2-3 hr
- **狀態**：explore 完成，deferred 等下個 session Sign-on

### Sign E deferred — GCP service account key rotate

- `google-service-account.json` 建立日期：2026-07-16（6 天 old，仍 fresh）
- SOP §1：90 天 rotate 標準（剩 84 天寬限）
- §7 自動化建議：`scripts/key_age_check.sh` + cron 排程（已寫進 SOP，未執行 cron 設定）
- **狀態**：deferred，等 60 天 warn / 90 天 critical 再觸發

### Round 2 文件清理 + 資安（commit `810c91b`）

| 文件 | 修改 |
|------|------|
| `docs/TESTING_GUIDE.md` | 加 §0.5 `.netrc` 安全設定 + 修正 §1.2 / §1.3 / §1.4 / §1.6 / §1.7 |
| `HANDOFF.md` | §5 P1 框架改 partial done + 加 ✅ Round 14/Round 1/Round 2 完成 block |
| `memory/2026-07-20.md` | append Round 1+2 segment (+28 lines) |

**資安檢查（sign-on 22:30）**：
- `secrets/` 目錄 ✅ mode 700
- 4 個 secret 檔案 ✅ mode 600（dashboard-pwd / api-pwd / api-token / line-bot-token）
- `gmail-credentials.json` / `google-service-account.json` ⚠️ mode 664 → 建議 chmod 600（Hubert sign-on 待做 = Sign H）
- src/scripts grep 明文密碼 ✅ 無
- commit history grep 明文密碼 ✅ 無（只有 `.env.example` placeholder）
- `.netrc` 推薦 ✅ `curl -n` 取代 `-u user:pass`

### 🎓 教訓整合（7 條）

1. **PRODUCTION CRASH ALREADY EXISTED** — `orderFormatter.js` exports `formatOrderSummary`，但 `src/index.js` + `confirming.js` require undefined `formatCustomerReply`。Hubert 報 B16 之前 production 走到 `all_fields_received` 直接 crash。我加 alias `formatCustomerReply = formatOrderSummary` 同時修 B16 + 避免生產 crash。

2. **LLM 自己推理時間邏輯是個 anti-pattern** — `main_idea.md` 已 prompt 但 7/20 20:00 仍建議 7/21 訂購。Hard-call `validateDate` + 列 7 reject 路徑讓 LLM 看到時不敢自己拍腦袋。

3. **.netrc 取代 cmdline 明文密碼** — `ps aux` 看得到 `-u user:pass`。`curl -n` 從 `~/.netrc` 讀（mode 600）安全。

4. **TESTING_SOP 不要 `date -d '+7 days'`** — 開團日是固定清單（`open_dates`），不是「今天 +7 天」邏輯。從 config 抓真實 next open date。

5. **CLI Python query fragile** — `count=0` 時 IndexError。grep + regex view 更 robust。

6. **test 轉換用 `node:test` pattern 易踩 syntax** — `tests/timezone.test.js` line 8 被 corrupt 成 `require('node:test';)` 缺 closing paren，立即 `sed` 修回 + amend。**SOP**：轉換完每個檔立即 `node -c` 驗證 syntax。

7. **lint:fix 是 session 收尾 SOP 的好朋友** — `tests/logger.test.js` line 149-150 `no-multi-spaces` 被 catch，`:fix` 一鍵解，且 CI push 阻擋需 0 errors。

### Final state（commit `5ca4aba`，23:08）

```
HEAD chain（本 session 新 commits）：
5ca4aba  Sign C-all timezone.test.js syntax fix (amend)
a649467  Sign C-all 第 8 批 3 套 — 48/48
097e657  Sign C-all 第 7 批 8 套
9fe0726  Sign C-all 第 6 批 6 套
595c557  Sign C-5 states 系列 4 套
51f4c3c  Sign C-4 rules 系列 9 套
9adba15  Sign C-3 第 3 批 5 套
0a9cd0a  Sign C-2 第 2 批 3 套
e55767c  docs(handoff) Sign B done
810c91b  Round 2 文件 + 資安
6dabe71  P1 B16 + P0 #4 (formatCustomerReply alias + main_idea.md 日期強化)
0a9214a  P0 #2 解除轉真人按鈕
8704387  P0 #1 dashboard 重新生
23091c4  Bug #1 integration test
e5f8564  Bug #1 tests 適配
98151cf  Bug #1 root cause
51e3dcc  Sign C-1 第 1 批 5 套 (5/48)
...(更早 Round 14 commits)...
```

**check-quality（23:10 後）**: 12 通過 / 1 警告 / 0 失敗
- 警告（保留）：Check 10 `AGENTS.md` drift（production runtime ↔ docs/production-prompt/2026-07-03）— 已記在 SOP §3.2，下個 session 第一件事決策

**npm test**: 53 個測試檔 100% pass

**Worker**: deploy v `683f6f9b-ec22-4c1b-b96e-a6b5f39b974c`（Bug #1 雙邊 fix 生效）

### Session Close (2026-07-22 23:08 GMT+8)

**操作流程**：
1. ✅ `bash scripts/check-quality.sh` 確認 11/1/2 → `npm run lint:fix` → 預期 12/1/0
2. ✅ 寫本文檔（`memory/2026-07-22.md` 整合）
3. ✅ 更新 `docs/handoff/sessions/SESSION_NEXT_PROMPT.md`（下個 session 開局 prompt）
4. ✅ 更新 `HEARTBEAT.md` + `memory/heartbeat-state.json` + `.task-state/active-tasks.md`（狀態檔防 drift）
5. ✅ git add -A + commit "docs(session-close): 2026-07-22 Round 15 close-out + Sign C-all 48/48 + handoff update"
6. ✅ push + rsync

### Hubert 下次 Sign-on options（待選擇）

- **Sign D**：Worker FAQ 前處理實作（2-3 hr，知識源已備好）
- **Sign E**：GCP service account key rotate（依 §7 SOP，key 還 84 天寬限）
- **Sign G**：修 Check 10 AGENTS.md drift（依 `docs/SYSTEM_AUDIT_2026-07-19.md` §3.2 決策哪邊是 source of truth）
- **Sign H**：`chmod 600` `gmail-credentials.json` / `google-service-account.json`（2 個檔，1 分鐘）
- **Sign 其他**：別的（請描述）

## Tags
#sign-c-all-test-framework-unification #node-test-pattern #production-crash-alias-formatCustomerReply #llm-date-logic-anti-pattern #netrc-replace-cmdline-passwords #sign-b-worker-deploy #bug1-cascade-b08-b10-b13-b17 #hardened-secret-chmod-600 #4-day-marathon-session

## Resources
- **16 commits**（從 51e3dcc 到 5ca4aba）：51e3dcc / 0a9cd0a / 9adba15 / 51f4c3c / 595c557 / 9fe0726 / 097e657 / a649467 / 5ca4aba + e55767c / 810c91b / 6dabe71 / 0a9214a / 8704387 / 23091c4 / e5f8564 / 98151cf
- **Sign B Worker Version**：`683f6f9b-ec22-4c1b-b96e-a6b5f39b974c`（Bug #1 雙邊 fix 生效）
- **Sign C-all 48/48 套**：從自訂 assert 風格統一為 `node:test` 風格（分 8 批 + 1 amend）
- **npm test 53 個測試檔 100% pass**
- **check-quality 12 通過 / 1 警告 / 0 失敗**
- **資安檢查**：
  - secrets/ 目錄 mode 700
  - 4 個 secret 檔案 mode 600（dashboard-pwd / api-pwd / api-token / line-bot-token）
  - 2 個 secret 檔案 mode 664（gmail-credentials.json / google-service-account.json）— 待 Sign H
- **Bug #1 雙邊 fix**：chicken.yaml + Worker `DEFAULT_IGNORED_KEYWORDS` 都移除「我要訂購」
- **formatCustomerReply alias**：修 B16 + 同時避免生產 crash（PRODUCTION CRASH ALREADY EXISTED）
- **7 大教訓**：
  1. PRODUCTION CRASH ALREADY EXISTED（formatCustomerReply alias）
  2. LLM 自己推理時間邏輯是個 anti-pattern
  3. .netrc 取代 cmdline 明文密碼
  4. TESTING_SOP 不要 `date -d '+7 days'` — 固定 open_dates 清單
  5. CLI Python query fragile — grep + regex 更 robust
  6. node:test pattern 易踩 syntax — 轉換完立即 `node -c` 驗證
  7. lint:fix 是 session 收尾 SOP 的好朋友
- **知識源已備好**：knowledge/tenants/chicken/12 個檔（01_product → 12_reply_examples + INDEX.md）
- **下次 Sign-on options**：D (Worker FAQ 2-3hr) / E (GCP key rotate 84天寬限) / G (AGENTS.md drift) / H (chmod 600 secrets)
- **2d 12h+ 長時間 session**：60+ 小時