# 2026-07-19

## What I Learned
- Cloudflare Dashboard connector 安裝的 tunnel `brt1122-System-09` 可 reuse 給雞味客服 dashboard，不需要新建 tunnel
- Dashboard tunnel 改用 systemd 自動管理（named tunnel + UUID），不再依賴 crontab 自動重啟
- 4 個 announce cron 的 `delivery.channel` 必須為平台名稱（如 `discord`），不是 channel ID
- Cloudflare Worker compatibility_date 升到 2026-07-01 + 移除 account_id（wrangler v4 自動偵測）
- Session 結束 SOP 文件化的價值：觸發關鍵字清單讓下個 session 自動做 close-out

## Notes

### 工作總覽：Round 9-14（整天）

7 個 round 從 Round 9 文件 drift 收尾 → Round 10 H/L 級修正 → Round 11 GCP drift → Round 12 L1 watchdog → Round 13 Named Tunnel 規劃 → Round 14 Named Tunnel 執行 + 4 cron 修復。

### Round 9（03:00+）— 第一輪整理（文件 drift 收尾）

**Commits**: `a4c2c36` → `e7bcac7`

- 文件 drift 收尾（CHANGELOG.md 重複 [Unreleased] 段合併 + race condition 修法 + .bak 清理）
- 完整 audit 報告：`docs/SYSTEM_AUDIT_2026-07-19.md`（16 KB）
- 補 `README.md`（8.5 KB）+ SESSION_BACKGROUND.md LEGACY 標頭

### Round 10（03:36+）— H/L 級 + 重要修正

**Commits**: `e280f90` → `36a8576` → `fadb6ec`

- **H1**: `scripts/manage-tunnel.sh start()` 帶完整 env + 用 `_FILE` 取代明文密碼
- **H2**: `scripts/check-quality.sh Check 10` 擴展 canonical drift 檢查
- **重要修正 1**: `scripts/sync-canonical.sh` 新增 + AGENTS.md 同步
- **重要修正 2**: `docs/GCP_ROTATION_SOP.md` 新增
- **重要修正 3**: `SESSION_H8_PROMPT.md` 狀態對齊（⏸ → ✅ + 4 commits 證據）
- **L1**: 攏長檔案加 LEGACY 標頭 + 接手者必跳過清單
- **L2**: production runtime `.bak` 清理評估
- **Session 結束 SOP**：`docs/SESSION_END_SOP.md`（129 行 SOP + 觸發關鍵字）

### Round 11（08:00+）— GCP drift 修正

**Commits**: `a21353a`

- 確認原本 `google-service-account.json` 是 3 天前建立的，仍可訪問 Sheets
- 修正 audit drift 誤判（不是 rotate 需求）
- 刪除 `google-service-account.json.new`（不需要）
- 更新 `docs/GCP_ROTATION_SOP.md §6.5 Audit Drift 說明`

### Round 12（08:14+）— L1 + watchdog 驗證

**Commits**: `e1d4ddb`

- L1 走 A+B+C 方案：3 個攏長檔案加 LEGACY 標頭
- watchdog 自動驗證成功（07:39 自動重啟 tunnel 4 秒內恢復）

### Round 13（08:23+）— Named Tunnel 轉移規劃

**Commits**: `36a8576`（Session 結束 SOP）+ 之後 commit

- 觸發關鍵字機制
- Cloudflare Worker audit 開始

### Round 14（08:30+ → 22:30+）— Named Tunnel 轉移執行

**Commits**: `fadb6ec` → `2cc89d1` → `c96214e` → `09ff830` → `38b1a27` → `901b7a8` → `8f8d1f7` (external-user repo)

#### 雞味客服 repo

- `fadb6ec`: Named Tunnel 轉移（scripts/manage-tunnel.sh + dashboard-watchdog.sh + docs/NAMED_TUNNEL_MIGRATION.md）
- `2cc89d1`: NAMED_DOMAIN 註解更新（brt1122.com 確認）
- `c96214e`: NAMED_TUNNEL_MIGRATION.md 步驟 2 精確化
- `09ff830`: NAMED_TUNNEL_MIGRATION.md 修整（reuse brt1122-System-09）
- `38b1a27`: dashboard tunnel 改用 brt1122-System-09 + 4 個 cron delivery 修復

#### external-user/cloudflare-worker repo

- `8f8d1f7`: Worker audit v2（wrangler.toml + DEPLOYMENT.md v2）+ actual deploy `e919157f`

#### 重要發現

- Cloudflare Dashboard connector 安裝建立了 tunnel `brt1122-System-09`（UUID `256e22ec-d01f-4f78-83f6-c929889173eb`）
- 從 2026-05-02 跑（78+ 天穩定，PID 1543 systemd service）
- **不需要新建 chicken-dashboard tunnel**，直接 reuse 已穩定的 `brt1122-System-09`
- Dashboard Public Hostname 已設定：`dashboard.brt1122.com` → `http://localhost:3000`

#### 系統狀態（最終）

- `/healthz`: dashboard / api_server / worker 全 up
- Cloudflare Worker: deploy v `e919157f`
- Dashboard tunnel: `brt1122-System-09`（systemd 自動管理）
- Dashboard URL: `https://dashboard.brt1122.com`（固定）

#### Cron 狀態變更（23:14+ 23:20+ 修整）

| Cron | 變更 |
|------|------|
| `36d2ca19` 雞味客服 dashboard watchdog | ❌ 已停用（22:48 Hubert）|
| `955d61c6` cloudflared leaked cleanup | ⚠️ error → ✅ 修 delivery channel |
| `796afb16` 日報彙總 | ⚠️ error → ✅ 修 delivery channel |
| `dc5afd05` 週報彙總 | ⚠️ error → ✅ 修 delivery channel |
| `6033de71` P9 Sheets 同步 | ⚠️ error → ✅ 修 delivery channel |

**修復方式**：
```bash
openclaw cron edit <id> --channel discord --to channel:1528418702167638016
```

下次觸發（依各自排程）會正常 announce 到 channel `1528418702167638016`。

### 文件更新狀態（23:25 最終）

#### ✅ 已更新
- `HEARTBEAT.md`（system-level）— 加 Round 14 變更
- `memory/2026-07-19.md`（本檔）— Round 9-14 完整紀錄
- 4 個 cron delivery channel 修復

#### 🔄 待後續 session 處理（剩餘待辦）

1. **chicken repo 文件對齊**（用 exec 處理 sandbox 限制）：
   - `SESSION_NEXT_PROMPT.md` 加「Dashboard Tunnel SOP」+「重要 Cron 狀態」段
   - `HANDOFF.md` 更新最後日期 + §5 待辦
   - `CHANGELOG.md` 加 Round 11-14 變更
   - `PROJECT_INVENTORY.md §9` 待修整清單
   - `SYSTEM_AUDIT_2026-07-19.md §8` Round 14 狀態
2. **heartbeat-state.json** 更新（3+ 個月過時）
3. **驗證 dashboard.brt1122.com 對外可訪問**（等 Hubert 從 local 跑 curl）
4. **統一測試 framework** 到 `node:test`（48 個自訂 assert）
5. **L1 攏長文件 archive**（54 refs 跨檔，待規劃 references update）
6. **L2 production runtime `.bak` 清理**（明確清單在 SYSTEM_AUDIT §6）
7. **GCP service account key rotate**（雖 3 天前建立，仍建議 90 天 rotate）
8. **Cloudflare Worker staging 測試**（已 deploy production，建議先 staging）

### 關鍵決議記錄（給未來 session 參考）

1. **Dashboard tunnel 不再 zombie**：使用 brt1122-System-09 named tunnel + systemd 自動管理
2. **watchdog cron 不再重啟 tunnel**：改為「監控 + 記錄」模式（systemd 自動管理）
3. **4 個 announce cron 統一 deliver 到 channel `1528418702167638016`**（Hubert 指定）
4. **Cloudflare Worker deployment**：compatibility_date 升到 2026-07-01 + 移除 account_id（wrangler v4 自動偵測）
5. **下次 session 接手 SOP**：見 `docs/SESSION_END_SOP.md` 觸發關鍵字清單

## Tags
#round-9-to-14-cleanup #named-tunnel-brt1122-system-09 #systemd-auto-manage #cloudflare-dashboard-connector-reuse #cron-delivery-channel-fix #doc-drift-collection #legacy-file-headers #cloudflare-worker-audit-v2 #session-end-sop #heartbeat-drift

## Resources
- **13 commits**（Round 9-14）：
  - 雞味客服：a4c2c36 / e7bcac7 / e280f90 / 36a8576 / fadb6ec / a21353a / e1d4ddb / 2cc89d1 / c96214e / 09ff830 / 38b1a27
  - external-user：8f8d1f7（Worker audit v2 + deploy `e919157f`）
- **Named Tunnel**: `brt1122-System-09` (UUID `256e22ec-d01f-4f78-83f6-c929889173eb`)
- **Dashboard URL**: `https://dashboard.brt1122.com`（固定）
- **Worker deploy v**: `e919157f`
- **4 個 cron delivery 修復**：日報 `796afb16` / 週報 `dc5afd05` / Sheets sync `6033de71` / cloudflared cleanup `955d61c6` / watchdog `36d2ca19` 已停用
- **channel**: `1528418702167638016`（Hubert 指定）
- **新文件**：
  - `docs/SYSTEM_AUDIT_2026-07-19.md`（16 KB）— 完整 audit 報告
  - `docs/SESSION_END_SOP.md`（129 行）— SOP + 觸發關鍵字
  - `docs/GCP_ROTATION_SOP.md` — GCP key rotate 標準
  - `docs/NAMED_TUNNEL_MIGRATION.md` — tunnel 轉移步驟
  - `scripts/sync-canonical.sh` — AGENTS.md 同步
  - `scripts/manage-tunnel.sh`（H1 改進）— 帶 env + _FILE
  - `scripts/check-quality.sh` Check 10 — canonical drift 檢查
- **3 個 LEGACY 標頭**：雞味專案攏長檔案
- **剩餘待辦 8 項**：文件對齊 / heartbeat-state.json 更新 / dashboard URL 驗證 / test framework 統一 / L1 archive / L2 .bak 清理 / GCP key rotate / Worker staging
- **Hubert 在 Round 14 最後 22:48 停用 dashboard watchdog**：因為 tunnel 由 systemd 管理不需要再 watch