# 2026-06-26

## What I Learned
- 雞肉專案 src/ 不是 production runtime，真正 production 是 `~/.openclaw/agents/external-user/` 的 OpenClaw agent
- 永遠不要 `rm data/orders/*/*` 清理測試資料，必須用 PROTECTED 清單（保護清單機制）
- SOP 文件 vs 實情最大落差：接手者最容易被誤導，文件對齊是長期工程
- 「方向對 + 細心追求完整」要搭配「保護清單」這類硬規則才會不出錯

## Notes

### 工作總覽（12:15 → 22:28，10 小時）

**產出：**
- 1 份評估文件：`docs/TODO_2026-06-26.md`（完整 122 檔 audit）
- 8 個 git commit（HEAD = `f3ecfd4`）
- 18 套測試全綠（16 單元 + 2 整合）
- 5 套新測試 + 2 個新檔案（admin.html、cleanup-test-orders.sh）
- GitHub push + 主位置 rsync 同步兩邊都完成

### 重要發現 — 一定要記住

#### 雞肉專案 src/ 不是 production runtime ⚠️
- 真正的 production 是 `~/.openclaw/agents/external-user/` 的 OpenClaw agent
- src/ 只用於：邏輯驗證 + 單元測試對象
- SOP.md / INDEX.md / SPEC.md / REVIEW_GUIDE.md 都把 src/ 描述成 production runtime（這是文件 vs 實情最大落差）
- 接手者容易誤導，未來文件對齊需要修正

#### Production prompt 不在雞肉 git 倉庫
- 位置：`~/.openclaw/agents/external-user/SOUL.md` + `knowledge/main_idea.md`
- 不會被 commit 到雞肉 git
- 但會立刻影響 production runtime
- sync-mirror.sh `from-legacy` 會把主位置的 prompt 覆蓋回原位置
- 未來需要：定期把 production prompt 同步進雞肉 git 倉庫（`docs/production-prompt/`）作為 reference

### P0 修整（5 個全完成）

| ID | 主題 | 影響 |
|----|------|------|
| P0-1 | addressRule.js 對「超出配送範圍」說謊 | ✅ 真的觸發 handoff |
| P0-2 | handoff.js 的 customer_reply 沒讀 config | ✅ 從 config 讀 + DEFAULT fallback |
| P0-3 | stateMachine transition 覆蓋 trimmed 值 | ✅ fieldValue 優先於 value |
| P0-4 | scripts/admin.html 不存在 | ✅ 完整管理後台 UI |
| P0-5 | cognee_import.py 是 placeholder | ✅ 刪除（commit `c45c63b`）+ 更新 MEMORY.md |

### P1 修整（6 個完成）

- P1-1：CSV schema 文件統一（csvWriter.js 為 single source of truth）
- P1-2：addressRule 動態讀 loader.loadDeliveryAreas（不再 hardcode）
- P1-3：menuRule.parseItems 去重（兩個 regex 不再重複）
- P1-5：package.json test script 改用 glob（`node tests/*.test.js`）
- P1-6：community 欄位加進 awaitingInfo（FIELD_ORDER 順序）
- P1-7：測試套數文件統一（10 套）
- P1-8：dashboard-server.js js-yaml fallback（避免 npm install 漏跑就 crash）

### P2 修整（6 個完成）

- P2-1：刪除 reaskInfo.js（dead code）
- P2-2：handleConfirming 移除 export（dead code）
- P2-3：customer_reply event 從 stateMachine 刪除（dead code）
- P2-5：notifier/lineProfileCache 改用 config 介面（多租戶規模化必要）
- P2-7：legacy data/orders/2026-12-31.csv 刪除
- P2-8：PHASE1_PROGRESS.md 補充 2026-06-26 段
- P2-9：README.md 重命名為 MIGRATION_HISTORY.md + 新 README

### Production prompt 改動（4 個問題）

1. **不要說 AI** — SOUL.md 加「絕對禁止字眼清單」+ 修話術範例
2. **開團日期** — main_idea.md 第五 A 改為讀 config.yaml open_dates
3. **菜單圖片** — main_idea.md 第十一節加 3 張 R2 圖片 URL
4. **CSV 寫入** — 已驗證有寫入（22 筆 PENDING），但有 `user_line_name=Unknown` bug 已在 P2-5 修

### ⚠️ 慘痛教訓：兩次誤刪真實訂單

兩次不小心刪到 `data/orders/chicken/2026-06-13.csv` 跟 `2026-06-16.csv`（真實訂單）。原因是測試時建了訂單，用 `rm *.csv` 清理時把真實的也刪了。

**解法：** `scripts/cleanup-test-orders.sh` 內有 PROTECTED 清單（2026-06-13.csv + 2026-06-16.csv），只刪清單以外的。

**原則：** 永遠不要 `rm data/orders/*/*`，必須先列出檔案再決定刪哪些。

### 還沒做的（決策過但尚未執行）

- 將 production prompt 改動 commit 進雞肉 git（`docs/production-prompt/2026-06-26/`）作為 reference
- 環境測試：實際用真實 LINE 帳號驗證 prompt 改動的客戶體驗

### 工作方法論驗證

今天大量驗證了 v2 工作方法論：
- Actor-Critic 自我檢查清單：每次送出前跑（方向、內容、執行）✅
- 80% 規劃 / 20% 執行：先 TODO 文件再動手 ✅
- A 問 A 答：回報簡潔不回憶所有背景 ✅
- 細心追求完整、重視表達：但還是兩次誤刪訂單 → 驗證 SOP 不足

**新教訓：** 規劃再完整，沒有「保護清單」這類硬規則，還是會出錯。未來類似情境（測試 + 真實資料混存）必須一開始就建立 PROTECTED 清單。

### 雞肉專案未來 Roadmap（從 TODO 文件整理）

**短中期（下個 sprint）：**
- 修 addressRule 對 fallback 邏輯（loader 沒列的周邊市區如三峽、鶯歌需要 fallback）
- dashboard-server.js yaml.dump 會破壞格式（已標記為 P1-9，待修）
- API server addressRule 對「不在配送區域」會 inline 阻擋且說「已轉交人工處理」（P0-1 已修）
- 真實 LINE 測試整合

**中長期：**
- 6/16 失敗的 quick reply 流程（5 個方向 A-E 待評估）
- Google Sheets 取代 CSV（PHASE 2）
- 第二個客戶（鴨肉）建立 — 多租戶實際擴展驗證

### OpenClaw system 觀察

- GitHub：已登入 (`gh auth status` ✅)，push 順暢
- GitHub repo：`kaden1122123/chicken-group-buying-cs`（雞肉專案）
- OpenClaw agent：`.openclaw/agents/external-user/`（雞肉用）
- Production Worker：`~/openclaw-workspace/external-user/cloudflare-worker/`（Version f3ecfd4 之後版本）
- Cognee 知識圖譜：MEMORY.md 寫 ✅ 實際是 placeholder（已刪除，MEMORY.md 已更新）

### 下次 Session 建議

1. 開新 session 時讀取 `docs/TODO_2026-06-26.md` 了解專案現狀
2. 如果要繼續修整：先看 TODO 文件的 P1-9（dashboard-server yaml dump）跟剩餘 P2 項目
3. 如果要做 prompt 改動：在 `~/.openclaw/agents/external-user/` 改 + commit 進雞肉 git 的 `docs/production-prompt/`
4. 永遠先用 `scripts/cleanup-test-orders.sh` 清理測試訂單（保護清單機制）

## Tags
#chicken-group-buying #p0-fix #p1-fix #p2-fix #production-runtime #prompt-injection #cleanup-protected-list #todo-audit #multi-tenant #memory-reorganization

## Resources
- TODO 文件：`docs/TODO_2026-06-26.md`（完整 122 檔 audit）
- 8 個 git commit（HEAD = `f3ecfd4`）
- 18 套測試全綠（16 單元 + 2 整合）
- 5 套新測試 + 2 個新檔案（admin.html、cleanup-test-orders.sh）
- P0-5 cognee_import.py 刪除 commit：`c45c63b`
- Production prompt 位置：`~/.openclaw/agents/external-user/SOUL.md` + `knowledge/main_idea.md`
- PROTECTED 清單：2026-06-13.csv + 2026-06-16.csv