# 2026-06-30

## What I Learned
- 跨日工作 + 「以完成日為記錄日期」原則，08:23 修正後續寫入 `memory/2026-06-30.md`
- Session 結束時主位置 housekeeping 是必要 SOP（測試會寫 CSV 殘留）
- 5 個 housekeeping 修整中 3 個是 rsync 後殘留：測試 CSV + I5 fixture + chmod 漏 +x
- `timeUtils.test.js` 情境 4 失敗根因：依賴 `new Date()` 取「現在時間」沒 mock Date，下午跑會因 now > today 13:00 cutoff 失敗
- OpenClaw Cron 跑了 2 次（02:01:22 + 02:02:35，size 完全一樣 73 秒間隔）— cron 重複觸發

## Notes

### 工作總覽（6/29 16:00 → 6/30 09:41）

| 階段 | 時間 | 內容 | 結果 |
|------|------|------|------|
| 1. 專案掃描 | 06-29 16:02 | 完整掃描 + 給 Hubert 功能/缺口/文件狀態報告 | ✅ 12 Sessions + 5 個待修整項目列出 |
| 2. 程式碼修整 | 06-29 16:45 | 4 個 housekeeping commits | ✅ 4 commits pushed |
| 3. 主位置 5 項修整 | 06-29 17:38 / 18:57 | production runtime 5 項清理 | ✅ 全部完成 |
| 4. 文件更新 | 06-29 19:35 | PHASE1_PROGRESS.md + MEMORY.md L2 §I-5 | ✅ 1 commit pushed + 系統級 SOP 生效 |

**總計 5 commits pushed 到 origin/main**，從 `e7d6513` → `393364e`

### 階段 1：專案掃描（6/29 16:02）

**整體規模：**
- 程式碼 5,230 行（33 個 src 檔）
- 測試 30 套（29 in tests/ + 1 in scripts/）
- 文件 64 篇 + 12 個 scripts
- 真實訂單 `2026-06-13.csv` + `2026-06-16.csv` 持續 PROTECTED
- 最新 commit `e7d6513`

**12 個完成的 Sessions：** A-D、E、N、F、G、D3、D4、H、I、J、L、K、M

**發現的 5 個問題：**

1. 🔴 **Critical Bug**：`tests/timeUtils.test.js:99` 失敗
   - 情境 4（isWithinOrderTime）依賴 `new Date()` 取「現在時間」，沒 mock Date
   - 下午跑測試時「明天 14:00 配送」會 fail（因為 now > today 13:00 cutoff）
   - production code 邏輯正確，僅測試需 mock

2. 🔴 **Untracked 檔案**：
   - `.task-state/session-I/`（整個目錄）
   - `config/tenants/test-yaml-patch-i5.yaml`

3. 🔴 **Session J 空目錄**：`.task-state/session-J/` 只有 `.` 和 `..`

4. 🟡 **文件過時**：
   - `docs/INDEX.md`：寫 19 套測試，實際 30 套（最後更新 2026-06-26）
   - `REVIEW_GUIDE.md`：寫 29 套，實際 30 套
   - `docs/CLEANUP_PHASE_2_PLAN.md`：沒標 J/L/K/M 完成

5. 🟡 **測試殘留**（主位置）：
   - `data/orders/chicken/2026-06-18.csv` + `2026-06-29.csv`（測試 fixture 漏 commit）
   - 主位置還多 6 行 PENDING 訂單

### 階段 2：程式碼修整 — 4 個 Commits

**Commit `0559ac9` — fix(tests): 修 timeUtils.test.js 時間敏感性 bug**
- 從 `tests/date.test.js:23-35` 移植 `mockTime` + `restoreTime` helper
- 情境 4 改寫：先用 `RealDate` 建好所有 deliveryDate，再 mock 固定「今天 10:00」
- 情境 4 結束呼叫 `restoreTime()` 避免影響情境 5/6
- **production code `isWithinOrderTime` 邏輯不動**（已驗證正確）
- 測試描述也修對（原：cutoff = 明天 13:00 還在收單 → 改：cutoff = 今天 13:00）
- 驗證：`npm test` 29 套全綠 + lint 0 errors

**Commit `843a490` — chore(session-I): 補 retro state + commit I5 fixture**
- Session I 在 6/29 11:48 完成時漏 commit：
  - `.task-state/session-I/goal.md` + `steps.md`（23 + 59 行）
  - `config/tenants/test-yaml-patch-i5.yaml`（27 行）

**Commit `ec4b8b1` — chore(session-J): 補 retro goal.md + steps.md**
- Session J 在 6/29 12:08 完成時**完全忘了建** `.task-state/session-J/` 狀態檔
- retro 補建 51 + 48 行

**Commit `6898d81` — docs(consistency): 對齊測試套數 + 補 J/L/K/M 完成狀態**

3 份文件同步：
1. `docs/INDEX.md`（最後更新 2026-06-14 → 2026-06-29 16:50，測試套數 19 → 30）
2. `REVIEW_GUIDE.md`（測試套數 29 → 30，補 integration 章節）
3. `docs/CLEANUP_PHASE_2_PLAN.md`（標記 H/I/J/K/L/M 全部 ✅）

**Push + Rsync（6/29 17:23）：**
```
git push origin main   # 4 commits pushed: e7d6513 → 6898d81
bash scripts/sync-mirror.sh from-legacy   # 11 個檔案 sync 到主位置（80,477 bytes）
```
主位置驗證：`npm test` 29 套全綠 + lint 0 errors

### 階段 3：主位置 5 項修整

Hubert 17:38 指示「在主目錄還有看到什麼需要修整的部分一併修整」。

**修整 1：刪除測試 CSV**
```bash
node scripts/cleanup-test-orders.js
```
- 主位置 `data/orders/chicken/2026-06-18.csv`（899 bytes）+ `2026-06-29.csv`（6681 bytes）
- `2026-06-29.csv` 還比原位置多 6 行 PENDING 訂單
- **PROTECTED 機制驗證**：md5sum 確認 6/13 + 6/16 內容未變

**修整 2-3：刪除 Session I5 測試 fixture**
- `rm config/tenants/test-yaml-patch-i5.yaml`（485 bytes）
- `rmdir knowledge/tenants/test-yaml-patch-i5/`（空目錄）
- Session J 才加 `.rsync-filter`，這是 sync 進主位置的殘留

**修整 4：chmod +x manage-tunnel.sh**
```bash
chmod +x scripts/manage-tunnel.sh
```
2026-06-16 建立，其他 scripts 都有 +x 但這個漏了。

**修整 5：重新生成 dashboard.html**
```bash
node scripts/dashboard.js
```
從 2026-06-15 過時 → 2026-06-29 18:59 重新生成。

### 階段 4：文件更新 + MEMORY.md SOP

**Commit `393364e` — docs(chicken): 主位置 housekeeping 紀錄 + dashboard.html 重新生成**

- `PHASE1_PROGRESS.md`：加「主位置 Housekeeping」段（5 項修整 + 業務影響 + 經驗教訓）
- `dashboard.html`：從 2026-06-15 過時版本重新生成（2026-06-29 18:59）
- Push：`6898d81 → 393364e main → main` ✅

**MEMORY.md L2 §I-5（系統級 untracked，直接生效）**

新增「Session 結束時主位置 Housekeeping SOP」（2026-06-29 教訓整合）：

**觸發：** 任何 session 結束時（不限結構性變更 session，只要跑過 npm test）

**SOP 5 步（3 分鐘）：**
1. 主位置 cleanup：`cd ~/.openclaw/workspace-external-user/projects/chicken-group-buying-customer-service && node scripts/cleanup-test-orders.js`
2. 原位置 cleanup：`cd /home/clawuser/openclaw-workspace/others/chicken-group-buying-customer-service && node scripts/cleanup-test-orders.js`
3. 驗證主位置乾淨：`data/orders/chicken/` 只有 6/13 + 6/16
4. rsync from-legacy
5. 驗證主位置 production-ready：`npm test` 29 套全綠

**禁止：**
- ❌ 只 rsync 不 cleanup（會殘留測試 CSV）
- ❌ 信任 npm test 跑完就乾淨（測試會寫 CSV）
- ❌ 用 `rm -f` 暴力刪 CSV（可能誤刪真實訂單）
- ❌ 信任 `.gitignore` 排除就沒事（實體檔案仍在 production runtime）

**為何這個 SOP 重要：** 2026-06-29 17:38 Hubert 指示修整時發現 5 個項目中有 3 個是「rsync 後殘留」。根因是 brtclaw 在 session 期間沒主動跑 cleanup-test-orders.js，依賴 rsync 自動清，但 rsync 不會清 untracked 檔案。

### 今日 2026-06-30 早上操作

#### 02:00 OpenClaw cron 跑備份（異常：跑了 2 次）

**Job ID**：`bd933551`（每日 02:00 Asia/Taipei，Session M2 設定）

**實際備份檔案：**
```
~/.backups/chicken/2026-06-30/chicken-backup-20260630-020122.tar.gz  (20,213 bytes)
~/.backups/chicken/2026-06-30/chicken-backup-20260630-020235.tar.gz  (20,213 bytes)
```

兩個 size 完全一樣 + 73 秒間隔 → **同一個備份跑了 2 次**。可能原因：
- OpenClaw cron 重複觸發
- 系統層 retry
- agentTurn payload 重複 announce

**影響：** 不影響功能（只是浪費 20K 空間 + 1 個檔案）。7 天 rotation 後會清掉，但同一天兩個檔案會佔 40K。

#### 09:06 Hubert 問「現階段還有什麼沒處理的嗎？」

brtclaw 給了完整清單：
- 🟢 目前 production-ready（沒什麼需要立即處理）
- 🟡 2 個小訊號：備份雙跑 + 14 天舊 quick tunnel
- 🟢 中長期未做（9 個項目）

### Session Q：22:23 從 A 方案實測發現的 4 個問題

Hubert 早上實測 A 方案時發現：
- 客戶詢問菜單時沒收到 3 張 R2 圖片（只有純文字菜單）
- 出現錯誤：`create folder ~/.openclaw/workspace-external-user/memory/ → list files failed`
- 傳送兩則訊息，回覆卡住

**3 個 R2 圖片 URL：**
- `menu/menu_主推肉品.jpg`: `https://pub-ce7f744c6a2145a4a3277e8ed2c3f8fd.r2.dev/menu/menu_%E4%B8%BB%E6%8E%A8%E8%82%89%E5%93%81.jpg`
- `menu/menu_秘製小菜.jpg`: `https://pub-ce7f744c6a2145a4a3277e8ed2c3f8fd.r2.dev/menu/menu_%E7%A7%98%E8%A3%BD%E5%B0%8F%E8%8F%9C.jpg`
- `menu/menu_限量加購品項.jpg`: `https://pub-ce7f744c6a2145a4a3277e8ed2c3f8fd.r2.dev/menu/menu_%E9%99%90%E9%87%8F%E5%8A%A0%E8%B3%BC%E5%93%81%E9%A0%85.jpg`

#### Q1：目前隔離了哪些關鍵字（6 個）

```
- 我要訂購
- 菜單
- 常見問題
- 黑羽放山雞介紹
- 蔥鹽醬介紹
- 吃法介紹
```

**設計目的：** LINE 圖文選單自動回應（不讓 LLM 重複回），節省 token。Worker STEP 4.4 對這些完全比對 silently dropped（不轉 LLM）。

**實際問題：** LINE 圖文選單設定可能沒回 3 張菜單圖（而是回純文字），所以客戶看到的是 LINE 平台 fallback，不是 LLM 回應。

#### Q2：Dashboard 有，但目前沒在跑

- `scripts/dashboard-server.js` + `scripts/admin.html`（P0-4 修整）
- HTTP Basic Auth（帳：`admin`，密碼看 chicken.yaml）
- 功能：看訂單、編輯、yaml 字串 patch 改設定、狀態資訊
- 啟動：`node scripts/dashboard-server.js`
- 對外訪問：透過 `manage-tunnel.sh start`（Cloudflare Quick Tunnel）
- Session Q 的 Q4 會啟動 + 給 URL

### SESSION_Q prompt（commit `1211c3c`，251 行）

**4 個問題根因摘要：**

1. 🔴 **菜單沒傳圖片**（2 個根因）：
   - Sandbox 看不到新版 `main_idea.md`（`/workspace/knowledge/` 是舊版，沒圖片章節）
   - Worker `ignored_keywords` 把「菜單」silently dropped，LINE 圖文選單設定可能沒回圖

2. 🟡 **記憶體路徑錯誤**：`AGENTS.md` 寫的 `~/.openclaw/workspace-external-user/memory/` 跟實際 `~/.openclaw/agents/external-user/memory/` 不一致

3. 🟡 **回覆卡住**：從 session log 看 LLM 每則都有回，可能是 LINE 推送延遲或 reply token 過期（待查 pipeline）

4. 🔵 **Dashboard 未啟動**：見上面

**Session Q 修整計畫：**
- Q1：同步新版 main_idea.md + 從 `ignored_keywords` 移除「菜單」（或 LINE rich message）
- Q2：修 AGENTS.md memory 路徑
- Q3：查 Worker → OpenClaw → LINE pipeline
- Q4：啟動 dashboard + 對外 URL

### A 方案實測準備

Hubert 09:41 問「我現在要實測真實 LINE 帳號，但我忘記你說的 A 方案是什麼」。

**A 方案核心流程**（Session E 決策、Session N 上線，2026-06-28 19:30）：

```
1. 客戶發「我想訂購」 → LLM 詢問品項、日期、時段、地址、付款
2. LLM 整理訂單摘要（純文字） + 加「請回覆「確認」」提示
3. 客戶打「確認」關鍵字（確認/好/收到/yes/ok/送出/提交/訂購/下單/go/confirm）
4. LLM 整理最終訂單摘要 + push 通知 Hubert（含完整訂單資料）
5. LLM reply 客戶「訂單已收到，老闆會 24h 內聯絡您」
6. Hubert 看到 push 通知 → 手動到 dashboard 建單
```

**為何叫「過渡期」**
- 原本想走 D 方案（純 postback Worker 觸 API），但探索發現 3 個架構難題：
  1. Worker 拿不到 LLM 對話歷史（OpenClaw session 在 SQLite）
  2. OpenClaw 雞肉 agent 沒有 tool calling 機制
  3. `handlePostbackEvent` 函式從未實作（v1.5 隱藏 bug）
- A 方案 30 分鐘可上線，零架構風險，立即解除 12 天阻塞
- 缺點：Hubert 要手動建單（每天 5-10 分鐘）

**升級路徑**
- **Session O（B 方案）**：OpenClaw tool calling（LLM 自動呼叫 API 寫入訂單）
  - 觸發：每日 push 通知 > 5 筆 / Hubert 每日手動建單 > 15 分鐘
- **Session P（C 方案）**：OpenClaw ↔ Worker KV 同步（架構最完整版）
  - 觸發：每日 > 20 筆 / 需即時訂單狀態查詢

### 統計總覽（2026-06-29 16:00 → 2026-06-30 09:41）

| 指標 | 值 |
|------|---|
| Commits pushed | 5 個（0559ac9 / 843a490 / ec4b8b1 / 6898d81 / 393364e）|
| 主位置修整項目 | 5 項（全部完成）|
| 文件更新 | 4 份（PHASE1_PROGRESS.md / REVIEW_GUIDE.md / docs/INDEX.md / docs/CLEANUP_PHASE_2_PLAN.md）|
| MEMORY.md 新增段 | L2 §I-5（系統級 SOP）|
| 程式碼改動行數 | +63 / -32（timeUtils.test.js）+ 47 / -1（PHASE1_PROGRESS.md）|
| 測試套數 | 29 套全綠 + lint 0 errors |
| Push commits | 4 → 5（從 e7d6513 推到 393364e）|
| 主位置 rsync 同步 | 3 次 |
| 真實訂單保護 | ✅ 持續（md5sum 驗證）|

### 待辦 / 待跟進

- ⏸ **Hubert**：真實 LINE 帳號實測 A 方案（09:41 開始，發現 4 個問題 → SESSION_Q）
- 🟡 備份雙跑原因追查（02:01:22 + 02:02:35）
- 🟢 clawuser 的 14 天舊 quick tunnel（`kill 1926866`）
- 🟢 中長期：CHANGELOG.md / husky / staging / Sentry / E2E / Session O/P 升級
- 🟢 Session Q：菜單圖片 + AGENTS.md 記憶體路徑 + Worker pipeline + Dashboard 啟動

### 給明天的 brtclaw 提醒

1. **開 session 時**：讀本檔 + `memory/2026-06-29.md` + `.task-state/` 了解進度
2. **session 結束時**：跑 MEMORY.md L2 §I-5 housekeeping SOP（5 步 3 分鐘）
3. **每次 commit SOP**：git add -A + status + stat + commit + show
4. **brtclaw 你自己也要休息**：不要像 6/28 一樣跨日工作

## Tags
#cross-day-work #housekeeping-sop #memory-path-fix #test-residue #rsync-stale #timeutils-mock #openclaw-cron-double-run #session-q #a-plan-testing #menu-images-missing #dashboard-not-running

## Resources
- **5 commits 跨 6/29-30**：0559ac9 / 843a490 / ec4b8b1 / 6898d81 / 393364e
- **SESSION_Q prompt**（commit `1211c3c`，251 行，**待 push**）
- **MEMORY.md L2 §I-5**（系統級 untracked，5 步 housekeeping SOP）
- **隔離關鍵字**（6 個）：我要訂購 / 菜單 / 常見問題 / 黑羽放山雞介紹 / 蔥鹽醬介紹 / 吃法介紹
- **3 個 R2 菜單圖片 URL**：menu_主推肉品 / menu_秘製小菜 / menu_限量加購品項
- **Cron Job 異常**：bd933551 跑了 2 次（02:01:22 + 02:02:35）
- **Dashboard 現況**：scripts/dashboard-server.js + admin.html（P0-4 修整完成），但**目前未啟動**
- **12 個完成的 Sessions**：A-D / E / N / F / G / D3 / D4 / H / I / J / L / K / M
- **OpenClaw 路徑不一致**：`~/.openclaw/workspace-external-user/memory/`（AGENTS.md）vs `~/.openclaw/agents/external-user/memory/`（實際）
- **測試套數演進**：11 → 17 → 18 → 19 → 22 → 26 → 29 → **30** 套全綠
- **A 方案 production prompt**：`docs/production-prompt/2026-06-28/main_idea.md`（已 sync 到 production runtime）