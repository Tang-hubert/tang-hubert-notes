# 2026-06-14

## What I Learned
- 雞肉專案 `src/index.js` 不是 production runtime，實際訊息流是 `LINE → Worker → OpenClaw Gateway → external-user agent → LLM`
- production bug 修復必須修 OpenClaw external-user agent 設定/Cloudflare Worker，不是雞肉專案 src/
- OpenClaw delivery hook 有 bug：AI 回覆卡在 `pendingFinalDelivery` 機制，3 小時未送達
- 規模化要預先設計：tenant-aware 路徑抽象（chicken.yaml → 第二客戶）比後期 refactor 便宜 100 倍

## Notes

### 時程表

| 時間 (GMT+8) | 事件 |
|--------------|------|
| 08:00 | 收到任務：完整了解雞肉團購客服背景 |
| 08:30 | 提交初次 review（發現 src/ 沒在 production 跑）|
| 09:00+ | 修 Bug #0 (Worker 加 IGNORED_KEYWORDS) + 5 項技術債 |
| 09:40 | ✅ Worker 部署成功（Version `ef63e075`）|
| 09:41 | Hubert 實測 ignored_keywords 通過 |
| 12:26 | Hubert 決策：先修程式碼再修 prompt、SOP 完整版、規模化、儀表板 A |
| 14:30-14:50 | 執行 6 個批次（commit 4 個）|
| 20:09 | Hubert 朋友測試卡住 |
| 20:25-21:09 | 排查發現 OpenClaw delivery hook bug |

### 關鍵發現：production runtime 認知

**雞肉專案 `src/index.js` 在 production 沒跑！** 實際訊息流：
```
LINE → Cloudflare Worker → OpenClaw Gateway → external-user agent → LLM
```

`src/` 實質上是：
- ✅ 設計規格的程式化版本
- ✅ 單元測試對象（4 套測試，90+ 案例）
- ❌ 不是 production runtime

**教訓：** 雞肉專案 100% 測試通過但 production 有 delivery bug（測試通過 ≠ production 行為正確）

### Phase 1 Review + Bug #0 修復

**Bug #0：** `config.yaml.ignored_keywords` 原本只在設計層生效
**修復：** Cloudflare Worker 加 `IGNORED_KEYWORDS` 攔截
**部署：** Version `ef63e075-d9a0-4433-898a-87bb10bc58c6`
**驗證：** `tests/integration.test.js` 確認 bundled Worker 含 6 個關鍵字

**Phase B 技術債清理（5+2 項）：**
- B1：商品資料單一源（`menuRule.js` 動態讀 `01_product.md`，21 品項）
- B2：notificationFormat 6 個 type label 對齊 transferRules
- B3：CSV 檔名格式統一（`filename_pattern` 改 `{date}.csv`）
- B4：config.js 完整 fallback YAML parser
- B5：handoff.test.js「取消吧」修復
- B6：stateMachine.buildCancelResult() 共用
- B7：awaitingInfo.js 統一 trim

**Phase C 新測試覆蓋：** config.test.js、whitelist.test.js、integration.test.js
**結果：8 套全綠**

### Bug 1/2 修補

**Bug #1：晚上訂購進入訂單確認環節**
- 根因（雙層）：
  - 程式碼層：✅ `dateRule.js` 邏輯正確
  - Prompt 層：❌ 缺失關鍵規則
- 12 種情境決策表：

| 配送日 = 今天 | 配送日 = 明天 | 配送日 = 後天或之後 |
|--------------|--------------|-------------------|
| < 13:00 → ❌ 不可 | < 13:00 → ✅ 可 | 任意 → ✅ 可 |
| >= 13:00 → ❌ 不可（past_cutoff_today）| 13:00-14:00 → ❌ 不可 | |
| | 14:00-18:00 → ❌ 不可 | |
| | >= 18:00 → ❌ 不可 | |

**Bug #2：錯誤訊息沒突出「下一個開團日」**
- 缺少 `getNextOpenDate()` 函數

**修補（4 phases）：**
- A1：`dateRule.js` 加 `getNextOpenDate` / `getNextOrderableOpenDate` / `formatDateWithWeekday`
- A2：改進 validateDate 錯誤訊息
- A3：`timeSlotRule.js` 加 `validateTimeSlotWithDate`（時段 × 配送日 合併驗證）
- A4：新增 `tests/date.test.js`（30+ 案例）
- A5：修 prompt `main_idea.md`「九、收單時間規則」
- A6：修 prompt `SOUL.md`「訂購截止時間」

### 多租戶規模化抽離

**Hubert 洞見：**
> 個人建議在規劃階段就要有規模化的打算，不然到時候有真正客戶後，客服系統也早已敲板不適合多用戶開發。

**檔案結構：**
```
config/tenants/chicken.yaml       # 雞肉客戶設定
knowledge/tenants/chicken/        # 12 個知識檔案
data/orders/chicken/              # 訂單資料
```

**介面抽象：**
```js
process.env.TENANT_ID = 'chicken'  // 預設向後相容
const TENANT_CONFIG_PATH = config/tenants/{TENANT_ID}.yaml
const LEGACY_CONFIG_PATH = config.yaml  // fallback
function resolveConfigPath() { ... }
```

**升級檔案：** config.js / knowledge/loader.js / csvWriter.js / csvReader.js / orderIdGenerator.js

### SOP 完整版（8 大章節 / 11 KB）

1. 系統概述（架構/特性）
2. 人工設定清單（25+ 項）
3. 客製化標記（環境變數 + 檢查清單）
4. 部署步驟（6 步驟）
5. 維運清單（8 項定期任務）
6. 故障排除（8 種問題 + 緊急應變）
7. 給接手者的指南（必讀 + 10 項驗收清單）
8. 常見任務 SOP（4 個檢查清單）

### 儀表板 MVP

`scripts/dashboard.js` + `dashboard.html`：
- 從 CSV 讀取訂單
- 6 個關鍵指標卡片（總訂單/營收/客單價/客戶數/新客/老客）
- 3 個 Chart.js 圖表（訂單趨勢/狀態分佈/熱門品項 Top 10）
- 最近 20 筆訂單表格

### OpenClaw delivery hook bug（20:09 發現）

**問題：** Hubert 朋友（`U117a0f0c89dcb4084df3c983bd863524`）測試客服時，AI 有回覆但沒送到用戶端

**完整時間軸：**

| 時間 (GMT+8) | 朋友發送 | 雞肉 Bot 處理 | 訊息送到 user？|
|--------------|---------|---------------|----------------|
| 19:57:12 | 「你好 我要訂購」| ✅ 13 秒後回覆 | ❌ 沒送 |
| 20:00:30 | 「雞肉價格」| ✅ 32 秒後回覆價格表 | ❌ 沒送 |
| 20:00:46 | 「有沒有菜單」| ✅ 25 秒後回覆完整菜單 | ❌ 沒送 |

`sessions.json` 中 `systemSent: False` — **3 小時的 AI 回覆全部卡在 `pendingFinalDelivery` 機制裡**

**各環節驗證：**

| 環節 | 狀態 |
|------|------|
| Cloudflare Worker | ✅ 活著 |
| Worker ignored_keywords | ✅ 有效（blocked:1）|
| Worker 攔截「你好 我要訂購」| ❌ 沒攔截（預期 100% 完全比對）|
| OpenClaw gateway | ✅ 活著（PID 1810234）|
| OpenClaw LLM 處理 | ✅ 正常（13-32 秒回覆）|
| OpenClaw 送 LINE reply | ❌ **卡住**（systemSent: False）|
| LINE token | ✅ 有效（/v2/bot/info 200）|
| LINE Push API | ✅ 有效（手動 push 成功）|

**真正原因：** OpenClaw 的 LINE delivery hook 沒有成功執行（可能：replyToken 30 秒時效過期後沒 fallback 到 push / 內部 delivery pipeline bug / WebSocket 中斷）

**立即處置：**
- ✅ 手動 LINE Push API 送測試訊息給朋友（ID: 618422438233113143）
- ⏸ 未重啟 OpenClaw gateway（Hubert 未授權；會中斷 30 秒）

**給 Hubert 的 3 個選擇：**
1. 現在重啟 OpenClaw gateway（30 秒中斷，清掉卡住的 delivery）
2. 不重啟，等 OpenClaw 自己 timeout
3. 保持現狀，先讓朋友重發訊息

### Git Commits（4 個新）

```
8db08d8 feat: D1 儀表板 MVP + E1-E2 文檔整理
e166afd docs: C1 完整 SOP
1801c83 feat: 多租戶規模化抽離（向後相容）
1f7ca5e fix: Bug 1/2 修補（dateRule + timeSlotRule + tests）
```

加上之前的：`12974eb`、`e8454ba`

### 給未來自己的提醒

1. production 是 OpenClaw external-user agent，不是 src/index.js
2. config.yaml.ignored_keywords 需在 Cloudflare Worker 中才能生效
3. 每次修改 prompt 都要重新部署 Worker 或更新 agent 設定
4. 測試通過 ≠ production 行為正確
5. OpenClaw 的 delivery hook 是黑盒子，卡住時需要重啟 gateway 才能清除
6. 多租戶規模化預先設計比後期 refactor 便宜 100 倍

### 重要決策記錄

| 決策 | 來源 |
|------|------|
| 修 Bug 先修程式碼，再修 prompt | Hubert 14:04 |
| SOP 完整版 | Hubert 12:26 |
| 時程按任務分，不按天 | Hubert 14:04 |
| 規模化抽象方案 3（最完整）| Hubert 14:04 |
| OpenClaw 規模化方案 C | Hubert 14:04 |
| Google Sheets 不做 | Hubert 14:04 |
| 儀表板先 A | Hubert 14:04 |
| 多租戶向後相容 | Hubert 14:04 |

## Tags
#chicken-group-buying #production-runtime #ignored-keywords #bug-fix #multi-tenant #sop #dashboard #openclaw-delivery-hook

## Resources
- Repo: `kaden1122123/chicken-group-buying-cs`
- 4 commits pushed: `8db08d8`, `e166afd`, `1801c83`, `1f7ca5e`
- Worker Version: `ef63e075-d9a0-4433-898a-87bb10bc58c6`
- Docs: `docs/SOP.md` (11 KB), `docs/MULTI_TENANT_DESIGN.md`, `PHASE1_PROGRESS.md`
