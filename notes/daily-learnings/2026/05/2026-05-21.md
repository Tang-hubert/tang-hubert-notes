# 2026-05-21

## What I Learned
- IG Token format differs: `IGAAU...` (Instagram) vs `EAA...` (Facebook) — must use correct format with correct API host
- Roles Token bypasses OAuth flow and is generated directly from Meta Developer "Roles" page

## Notes

### i-En IG Token Full Rebuild

**Background Problem:**
i-En's IG posting function was completely broken because:
1. Old OAuth Token (`META_INSTAGRAM_API_KEY`, format `EAAR...`) expired after 60 days
2. Attempted to re-run OAuth flow → hit `Invalid platform app`
3. Old API version (`graph.facebook.com/v21.0`) was no longer valid

### Cloudflare Tunnel OAuth Callback Setup

**Purpose:** Establish `callback.brt1122.com` for future OAuth automation refresh

**Completed:**
- Cloudflare Zero Trust tunnel `meta_dev_OAuth_url` (Healthy)
- Callback server at `/home/clawuser/ig-oauth-callback/`
- Route: `callback.brt1122.com` → `localhost:3000`
- Currently idle, available for future Token auto-refresh flow

**Technical Details:**
- Port 3000 is custom, can be changed later
- Cloudflare new UI (Zero Trust) removed `Public Hostname` tab → use `Add a route` → `Published application`

### IG Access Token Investigation & Verification

**Error Assumption Clarified:**
- ❌ Instagram Basic Display API's "Generate Token" button = Instagram OAuth flow (requires Business verification)
- ✅ Token from Meta Developer "Roles" page Instagram Tester = Roles Token (bypasses OAuth, directly valid)

**Roles Token Verification Results:**
```
Token variable: META_DEV_IEN_ACCESS_TOKEN
Token format: IGAAU... (Instagram-specific token)
Account: ien_vision (Media Creator type)
IG User ID: 27668783336054813

Permissions (complete):
  - instagram_business_basic ✅
  - instagram_business_content_publish ✅ (posting permission)
  - instagram_business_manage_messages ✅
  - instagram_business_manage_insights ✅
  - instagram_business_manage_comments ✅

Refresh test: ✅ Success (5171652 seconds ≈ 60 days)
Post test: ✅ Success (Post ID: 17911996428406533)
```

### i-En Codebase Full Rewrite

**Files Changed:**

| File | Change |
|------|--------|
| `config.js` | `IG_TOKEN`: `META_INSTAGRAM_API_KEY` → `META_DEV_IEN_ACCESS_TOKEN` |
| `.env` | `IG_USER_ID`: `17841448905632128` (wrong Page ID) → `27668783336054813` (correct IG ID) |
| `publisher.js` | API Host: `graph.facebook.com` → `graph.instagram.com` |
| `publisher.js` | API Ver: `v21.0` → `v18.0` |
| `ien_light_monitor.sh` | Token variable + API endpoint + version all updated |

**New Architecture:**
```
API Host: graph.instagram.com (no longer graph.facebook.com)
API Ver: v18.0
Token: META_DEV_IEN_ACCESS_TOKEN (Roles Token, 60 days valid)
IG User ID: 27668783336054813 (ien_vision)
```

**IG API Limitation:** Must attach image, plain text posting not supported (`image_url is required`)

### Monitor Script Alert Loop Fix

**Problem:** `ien_light_monitor.sh` continued to falsely report `Code 190` Token expired (old expired OAuth Token)
**Solution:** Monitor now reads `META_DEV_IEN_ACCESS_TOKEN`, API endpoint changed to `graph.instagram.com/v18.0`
**Verification:** Script execution result `✅ Three checkpoints normal, standby quiet`

### Key Meta IG API Knowledge

| Knowledge Point | Description |
|-----------------|-------------|
| Instagram Token Format | `IGAAU...` → use with `graph.instagram.com` |
| Facebook Token Format | `EAA...` → use with `graph.facebook.com` |
| Roles Token | Bypass OAuth, generated directly from Meta Developer, valid 60 days |
| OAuth Token | Requires App published + Instagram platform integration, bypass needs Business verification |
| `Invalid platform app` | Using Facebook App ID/Secret to call Instagram API, or App missing Instagram integration |
| Dev Mode Limitation | `instagram_content_publish` requires Business verification for full use (but Roles Token already has this permission) |

### Follow-up Items

- [ ] `META_DEV_IEN_ACCESS_TOKEN` needs refresh after 60 days (next deadline: ~2026-07-20)
- [ ] After App published, can re-run OAuth flow for official token (long-term solution)
- [ ] `META_INSTAGRAM_API_KEY` old expired token kept (backup)
- [ ] Cloudflare Tunnel `callback.brt1122.com` on standby (for future OAuth automation refresh)

### Tomorrow's Suggestions

1. Confirm i-En actual posting process (cron job) running normally
2. Observe tomorrow morning if there are still `Code 190` false reports
3. Record next Token refresh time (60 days later)

## Tags
#i-en #instagram-api #meta-developer #roles-token #oauth #cloudflare-tunnel #token-refresh #graph-instagram

## Resources
- i-En IG User ID: 27668783336054813 (ien_vision)
- META_DEV_IEN_ACCESS_TOKEN: IGAAU...
- callback.brt1122.com → localhost:3000 (Cloudflare Zero Trust tunnel)
- Token refresh deadline: ~2026-07-20